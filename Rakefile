# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/prowlsms.rb'

Hoe.new('prowlsms', Prowlsms::VERSION) do |p|
  # p.rubyforge_name = 'prowlsmsx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
